import React from 'react';
import Input from '../../../../common/Input/Input';

const SearchBox = () => {
    return (
        <>
            <Input
                id='autocomplete'
                placeholder=''
            />
        </>
    );
}

export default SearchBox;
